/**
 * Database Migrations
 * Using Supabase - migrations are managed directly in Supabase dashboard
 * This file is kept for reference but not used in the application
 */

// Migrations are managed through Supabase dashboard
// No need for local migration runner with Supabase
export const migrationsConfigured = true

/**
 * Ensure migration applied once per process.
 * With Supabase, migrations are managed via the dashboard.
 */
export async function ensureMigrations(): Promise<void> {
  // Supabase migrations are configured and managed via the dashboard
  // This function is a no-op
  return
}
